import { router } from "expo-router";
import { Button, Pressable, Text } from "react-native";

export default function ManageOldContract() {
  return (
    <Text style={{ textAlign: "center", fontSize: 20 }}>UP</Text>
    // <Button
    //   title="Tạo"
    //   onPress={() => router.push("oldContract/create")}
    // ></Button>
  );
}
